# Encryptor

### RU

Encryptor - простая утилита для шифрации данных используя данную схему `source text -> binary code -> AES256`. Обратно - `AES256 -> binary code -> source code`

AES256 использует пароль, что позволяет утвердить безопасность зашифрованных данных.
